# Too many .split('/')

## Beginning

At first, the array that had the piece and the color of the piece was an array with the path to get the image.
Inside the code, I would get the side or the piece using .split('/') to get the side, the piece or both.


## Problem

Searching for a way to make the code faster, I saw that I was using .split('/') too many times.


### Solutions

I thought about sending the string already separated, but I was already doing it.

What about saving the value in a variable?
That made the code faster, but still, I was using .split('/') a lot of times accross the code.

Then, I tried to change the array of strings.
Instead of using the path to get the image in the ui, what about an array with just the side and the piece?

The array changed from ['/side/piece.png', '/side/piece.png'] to [['side', 'piece'], ['side', 'piece']].
In order to see the image in the UI, I used "/" + imagePiece.join("/") + ".png".

With that, all .split('/') were eliminated, and now I had to compere the time of this code to the old one.
